import { ImgCarouselPipe } from './img-carousel.pipe';

describe('ImgCarouselPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgCarouselPipe();
    expect(pipe).toBeTruthy();
  });
});
